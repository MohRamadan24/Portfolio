services:
  frontend:
    build: .
    container_name: portfolio_frontend
    ports:
      - "3000:3000"
    volumes:
      - portfolio_data:/app/data
    networks:
      - portfolio-network
    restart: unless-stopped

networks:
  portfolio-network:
    driver: bridge

volumes:
  portfolio_data:
